type statusButton = 'default' | 'icon' | 'iconText';
const typesButtonConst = Object.freeze({
 default: 'default',
 icon: 'icon',
 iconText: 'iconText',
});
const contentSearchBar = Object.freeze({
 placeholder: 'search',
});
export type { statusButton };
export { typesButtonConst, contentSearchBar };
export { IMAGES } from './images.const';
export { CATEGORIES, COFFE_ITEMS } from './categories.const';
export { AVATAR } from './avatar.const';
export { statusIcon, typesIcon } from './icon.const';
